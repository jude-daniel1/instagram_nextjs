/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.jennexplores.com", "upload.wikimedia.org", "i.pravatar.cc"],
  },
};

module.exports = nextConfig;
